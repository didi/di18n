import React from 'react';

export default function Test() {
  return (
  <div>
  {/* this is a comment in JSX */}
    <Button  onClick={admin.name && updateCardUserInfo.bind(this, admin.name)}>拷贝</Button>
  </div>
  );
}
