import React from 'react';

export default function Test() {
  return (
  <div>
  {/* this is a comment in JSX */}
    <Button  onClick={admin.name && updateCardUserInfo.bind(this, admin.name)} className='啦啦啦呼呼呼'>拷贝</Button>
  </div>
  );
}
